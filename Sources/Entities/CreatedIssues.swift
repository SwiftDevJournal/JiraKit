// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import NaiveDate

/// Details about the issues created and the errors for requests that failed.
public struct CreatedIssues: Codable {
    /// Details of the issues created.
    public var issues: [CreatedIssue]?
    /// Error details for failed issue creation requests.
    public var errors: [BulkOperationErrorResult]?

    public init(issues: [CreatedIssue]? = nil, errors: [BulkOperationErrorResult]? = nil) {
        self.issues = issues
        self.errors = errors
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.issues = try values.decodeIfPresent([CreatedIssue].self, forKey: "issues")
        self.errors = try values.decodeIfPresent([BulkOperationErrorResult].self, forKey: "errors")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encodeIfPresent(issues, forKey: "issues")
        try values.encodeIfPresent(errors, forKey: "errors")
    }
}
