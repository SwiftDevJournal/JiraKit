// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import NaiveDate

/// The list of groups found in a search, including header text (Showing X of Y matching groups) and total of matched groups.
public struct FoundGroups: Codable {
    /// Header text indicating the number of groups in the response and the total number of groups found in the search.
    public var header: String?
    /// The total number of groups found in the search.
    public var total: Int32?
    public var groups: [FoundGroup]?

    public init(header: String? = nil, total: Int32? = nil, groups: [FoundGroup]? = nil) {
        self.header = header
        self.total = total
        self.groups = groups
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.header = try values.decodeIfPresent(String.self, forKey: "header")
        self.total = try values.decodeIfPresent(Int32.self, forKey: "total")
        self.groups = try values.decodeIfPresent([FoundGroup].self, forKey: "groups")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encodeIfPresent(header, forKey: "header")
        try values.encodeIfPresent(total, forKey: "total")
        try values.encodeIfPresent(groups, forKey: "groups")
    }
}
