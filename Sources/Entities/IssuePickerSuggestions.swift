// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import NaiveDate

/// A list of issues suggested for use in auto-completion.
public struct IssuePickerSuggestions: Codable {
    /// A list of issues for an issue type suggested for use in auto-completion.
    public var sections: [IssuePickerSuggestionsIssueType]?

    public init(sections: [IssuePickerSuggestionsIssueType]? = nil) {
        self.sections = sections
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.sections = try values.decodeIfPresent([IssuePickerSuggestionsIssueType].self, forKey: "sections")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encodeIfPresent(sections, forKey: "sections")
    }
}
