// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import NaiveDate

/// Issue type scheme with a list of the projects that use it.
public struct IssueTypeSchemeProjects: Codable {
    /// Details of an issue type scheme.
    public var issueTypeScheme: IssueTypeScheme
    /// The IDs of the projects using the issue type scheme.
    public var projectIDs: [String]

    public init(issueTypeScheme: IssueTypeScheme, projectIDs: [String]) {
        self.issueTypeScheme = issueTypeScheme
        self.projectIDs = projectIDs
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.issueTypeScheme = try values.decode(IssueTypeScheme.self, forKey: "issueTypeScheme")
        self.projectIDs = try values.decode([String].self, forKey: "projectIds")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(issueTypeScheme, forKey: "issueTypeScheme")
        try values.encode(projectIDs, forKey: "projectIds")
    }
}
