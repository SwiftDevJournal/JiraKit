// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import NaiveDate

/// JQL queries that contained users that could not be found
public struct JQLQueryWithUnknownUsers: Codable {
    /// The original query, for reference
    public var originalQuery: String?
    /// The converted query, with accountIDs instead of user identifiers, or 'unknown' for users that could not be found
    public var convertedQuery: String?

    public init(originalQuery: String? = nil, convertedQuery: String? = nil) {
        self.originalQuery = originalQuery
        self.convertedQuery = convertedQuery
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.originalQuery = try values.decodeIfPresent(String.self, forKey: "originalQuery")
        self.convertedQuery = try values.decodeIfPresent(String.self, forKey: "convertedQuery")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encodeIfPresent(originalQuery, forKey: "originalQuery")
        try values.encodeIfPresent(convertedQuery, forKey: "convertedQuery")
    }
}
