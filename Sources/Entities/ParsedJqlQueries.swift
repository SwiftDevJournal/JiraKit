// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import NaiveDate

/// A list of parsed JQL queries.
public struct ParsedJqlQueries: Codable {
    /// A list of parsed JQL queries.
    public var queries: [ParsedJqlQuery]

    public init(queries: [ParsedJqlQuery]) {
        self.queries = queries
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.queries = try values.decode([ParsedJqlQuery].self, forKey: "queries")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(queries, forKey: "queries")
    }
}
