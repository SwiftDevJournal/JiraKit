// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import NaiveDate

/// Details of the statuses being created and their scope.
public struct StatusCreateRequest: Codable {
    /// Details of the statuses being created.
    public var statuses: [StatusCreate]
    /// The scope of the status.
    public var scope: StatusScope

    public init(statuses: [StatusCreate], scope: StatusScope) {
        self.statuses = statuses
        self.scope = scope
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.statuses = try values.decode([StatusCreate].self, forKey: "statuses")
        self.scope = try values.decode(StatusScope.self, forKey: "scope")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(statuses, forKey: "statuses")
        try values.encode(scope, forKey: "scope")
    }
}
